# Package required for wifi functions
opkg install wpad 

# Packages required for wget+https
opkg install libustream-openssl ca-bundle ca-certificates

wget https://raw.githubusercontent.com/containercraft/openwrt-lxd/master/aux/wip.tar

# Add wifi device to lxd container
lxc config device add gateway wlp2s0 nic nictype=physical parent=wlp2s0 name=wlp2s0
opkg install pciutils # lspci
